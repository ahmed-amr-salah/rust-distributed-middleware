# Ignore Rust build artifacts
/target/
**/*.rlib
**/*.rmeta

# Optionally ignore Cargo.lock for libraries (recommended for libraries, not for binaries)
# Cargo.lock

# Ignore files generated by Rust tools like `cargo test` and `cargo bench`
*.profraw

# Optionally ignore editor or IDE-specific files
.vscode/
.idea/
*.iml

# ignore Encryption-images dir
Encryption-images/

images/